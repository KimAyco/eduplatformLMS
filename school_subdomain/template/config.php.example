<?php
/**
 * Subdomain configuration — copy to config.php on each school subdomain.
 */

return [
    // Use 'auto' on Hostinger (recommended) OR the full server path to main LMS
    // Example path: /home/u353641241/domains/kimayco.site/public_html
    'lms_root' => 'auto',

    // Main LMS public URL (no trailing slash)
    'lms_url' => 'https://kimayco.site',

    // School login code from school registration (must be active in database)
    'school_code' => 'YOUR-SCHOOL-CODE',

    // Optional: 'school_slug' => 'my-school',

    // Optional: share session cookie with main site (requires SESSION_COOKIE_DOMAIN in main .env)
    'session_cookie_domain' => '.kimayco.site',
    // Main site .env must also set SUBDOMAIN_LOGIN_SECRET (same random string) for login bridge

    'portal_title' => null,
    'tagline' => 'Your dedicated learning portal',
    'welcome_message' => 'Sign in with your school email to access courses, materials, assignments, and quizzes.',
    'primary_color' => '#0f6cbf',
    'logo_url' => null,

    'platform_name' => 'EduPlatform',
    'platform_url' => 'https://kimayco.site',
    'show_platform_link' => true,
    'support_email' => null,
];
